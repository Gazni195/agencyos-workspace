// Placeholder until we connect to the real Supabase project and generate
// accurate types from its actual schema (`supabase gen types typescript`).
// Using `any` here only loosens type-safety on Supabase calls themselves —
// it does not affect the rest of the app.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
